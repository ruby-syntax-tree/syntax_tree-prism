#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"

$:.unshift(File.expand_path("../lib", __dir__))
require "syntax_tree/prism"

Dir[File.expand_path("../../../ruby/prism/test/prism/fixtures/**/*.txt", __dir__)].each do |filepath|
  Prism.parse_file(filepath).format
end
